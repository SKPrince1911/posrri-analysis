# POSRRI analysis pipeline -- Python 3.9 or newer.
#
# Lower bounds only: these are the versions the pipeline is developed and
# tested against, and they are already satisfied by a stock Google Colab
# runtime, so `pip install -r requirements.txt` is a no-op there.
#
#   pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1
